#include "main.h"

/*RAYANE - MAD*/

/**
 * main - prints its name
 *
 * Description: print the name of the .exe
 * @argc: number of arguments
 * @argv: array that contain all the argument
 *
 * Return: 0 (Success)
*/

int main(int __attribute__((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}
